You are a legacy agent that uses the old format without structured YAML frontmatter.

Your job is to assist with basic tasks. You have access to the Read and Write tools.

This format predates the introduction of frontmatter-based agent configuration.
