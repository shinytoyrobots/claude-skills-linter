{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://skill-lint.dev/schemas/skill.schema.json",
  "title": "Claude Code Skill Frontmatter",
  "description": "JSON Schema for Claude Code SKILL.md plugin skill file frontmatter",
  "type": "object",
  "required": ["name", "description"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Unique identifier for the skill (kebab-case recommended)"
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable description of what the skill does"
    },
    "when_to_use": {
      "type": "string",
      "description": "Additional auto-invocation context, appended to description (SR-004)"
    },
    "argument-hint": {
      "type": "string",
      "description": "Hint shown to the user for the skill argument"
    },
    "arguments": {
      "oneOf": [
        { "type": "array", "items": { "type": "string" } },
        { "type": "string" }
      ],
      "description": "Positional argument names (space-separated string or list)"
    },
    "disable-model-invocation": {
      "type": "boolean",
      "description": "Whether to prevent the model from invoking this skill automatically"
    },
    "user-invocable": {
      "type": "boolean",
      "description": "Whether users can invoke this skill directly"
    },
    "background": {
      "type": "boolean",
      "description": "With context: fork, whether the forked skill runs in the background"
    },
    "model": {
      "type": "string",
      "description": "Model to use (a /model value, 'inherit', or a full model id)"
    },
    "allowed-tools": {
      "oneOf": [
        { "type": "array", "items": { "type": "string" } },
        { "type": "string" }
      ],
      "description": "List of tools the skill is permitted to use (array or comma/space-delimited string)"
    },
    "disallowed-tools": {
      "oneOf": [
        { "type": "array", "items": { "type": "string" } },
        { "type": "string" }
      ],
      "description": "Tools removed from the pool while the skill is active (array or string)"
    },
    "paths": {
      "oneOf": [
        { "type": "array", "items": { "type": "string" } },
        { "type": "string" }
      ],
      "description": "Glob patterns limiting when the skill auto-loads (array or string)"
    },
    "shell": {
      "type": "string",
      "enum": ["bash", "powershell"],
      "description": "Shell for !`command` blocks"
    },
    "license": {
      "type": "string",
      "description": "License identifier (part of the Agent Skills spec)"
    },
    "quality_level": {
      "type": "integer",
      "minimum": 0,
      "maximum": 3,
      "description": "Quality enforcement level for this file (0-3)"
    },
    "context": {
      "type": "string",
      "description": "Execution context for the skill (e.g. fork)"
    },
    "agent": {
      "type": "string",
      "description": "Agent mode or name to use when running this skill"
    },
    "effort": {
      "type": "string",
      "description": "Reasoning effort level for the skill"
    },
    "hooks": {
      "type": "object",
      "description": "Lifecycle hooks for the skill"
    },
    "compatibility": {
      "type": "string",
      "description": "Compatibility requirements or notes"
    },
    "metadata": {
      "type": "object",
      "description": "Arbitrary metadata for the skill"
    }
  },
  "additionalProperties": true
}
